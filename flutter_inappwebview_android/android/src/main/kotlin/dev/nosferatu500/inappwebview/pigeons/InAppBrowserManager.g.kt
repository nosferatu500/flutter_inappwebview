// Autogenerated from Pigeon (v27.3.2), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package dev.nosferatu500.inappwebview.pigeons

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object InAppBrowserManagerPigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Everything `InAppBrowserManager.open` puts into the browser Activity's `Bundle`.
 *
 * Exactly one of [urlRequest], [assetFilePath] and [data] is set, by `openUrlRequest`, `openFile`
 * and `openData` respectively.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class InAppBrowserOpenRequestData (
  /** The browser id: also the suffix of `InAppBrowserHostApi` / `InAppBrowserFlutterApi` (§195). */
  val id: String,
  /** `URLRequest.toMap()`, passed through as a map. It goes into the Bundle as `Serializable`. */
  val urlRequest: Map<String?, Any?>? = null,
  val assetFilePath: String? = null,
  val data: String? = null,
  val mimeType: String? = null,
  /** Handed to `loadDataWithBaseURL`, which ignores it on API 37 (§196). */
  val encoding: String? = null,
  val baseUrl: String? = null,
  val historyUrl: String? = null,
  /** `InAppBrowserClassSettings.toMap()`: decision B, a map for the Activity's `parse(Map)`. */
  val settings: Map<String?, Any?>,
  /** `ContextMenu.toMap()`, or an empty map when there is none, as before. */
  val contextMenu: Map<String?, Any?>,
  /**
   * Null when the browser was not opened for a window a page asked for. Kotlin stores it with
   * `putInt`, so it is narrowed there (checklist item 4).
   */
  val windowId: Long? = null,
  val initialUserScripts: List<Map<String?, Any?>?>,
  val pullToRefreshSettings: Map<String?, Any?>,
  /**
   * Each `InAppBrowserMenuItem.toMap()`. A map, which is also what avoids the
   * `AndroidResourceData` collision (checklist item 5).
   */
  val menuItems: List<Map<String?, Any?>?>
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): InAppBrowserOpenRequestData {
      val id = pigeonVar_list[0] as String
      val urlRequest = pigeonVar_list[1] as Map<String?, Any?>?
      val assetFilePath = pigeonVar_list[2] as String?
      val data = pigeonVar_list[3] as String?
      val mimeType = pigeonVar_list[4] as String?
      val encoding = pigeonVar_list[5] as String?
      val baseUrl = pigeonVar_list[6] as String?
      val historyUrl = pigeonVar_list[7] as String?
      val settings = pigeonVar_list[8] as Map<String?, Any?>
      val contextMenu = pigeonVar_list[9] as Map<String?, Any?>
      val windowId = pigeonVar_list[10] as Long?
      val initialUserScripts = pigeonVar_list[11] as List<Map<String?, Any?>?>
      val pullToRefreshSettings = pigeonVar_list[12] as Map<String?, Any?>
      val menuItems = pigeonVar_list[13] as List<Map<String?, Any?>?>
      return InAppBrowserOpenRequestData(id, urlRequest, assetFilePath, data, mimeType, encoding, baseUrl, historyUrl, settings, contextMenu, windowId, initialUserScripts, pullToRefreshSettings, menuItems)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      id,
      urlRequest,
      assetFilePath,
      data,
      mimeType,
      encoding,
      baseUrl,
      historyUrl,
      settings,
      contextMenu,
      windowId,
      initialUserScripts,
      pullToRefreshSettings,
      menuItems,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as InAppBrowserOpenRequestData
    return InAppBrowserManagerPigeonUtils.deepEquals(this.id, other.id) && InAppBrowserManagerPigeonUtils.deepEquals(this.urlRequest, other.urlRequest) && InAppBrowserManagerPigeonUtils.deepEquals(this.assetFilePath, other.assetFilePath) && InAppBrowserManagerPigeonUtils.deepEquals(this.data, other.data) && InAppBrowserManagerPigeonUtils.deepEquals(this.mimeType, other.mimeType) && InAppBrowserManagerPigeonUtils.deepEquals(this.encoding, other.encoding) && InAppBrowserManagerPigeonUtils.deepEquals(this.baseUrl, other.baseUrl) && InAppBrowserManagerPigeonUtils.deepEquals(this.historyUrl, other.historyUrl) && InAppBrowserManagerPigeonUtils.deepEquals(this.settings, other.settings) && InAppBrowserManagerPigeonUtils.deepEquals(this.contextMenu, other.contextMenu) && InAppBrowserManagerPigeonUtils.deepEquals(this.windowId, other.windowId) && InAppBrowserManagerPigeonUtils.deepEquals(this.initialUserScripts, other.initialUserScripts) && InAppBrowserManagerPigeonUtils.deepEquals(this.pullToRefreshSettings, other.pullToRefreshSettings) && InAppBrowserManagerPigeonUtils.deepEquals(this.menuItems, other.menuItems)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + InAppBrowserManagerPigeonUtils.deepHash(this.id)
    result = 31 * result + InAppBrowserManagerPigeonUtils.deepHash(this.urlRequest)
    result = 31 * result + InAppBrowserManagerPigeonUtils.deepHash(this.assetFilePath)
    result = 31 * result + InAppBrowserManagerPigeonUtils.deepHash(this.data)
    result = 31 * result + InAppBrowserManagerPigeonUtils.deepHash(this.mimeType)
    result = 31 * result + InAppBrowserManagerPigeonUtils.deepHash(this.encoding)
    result = 31 * result + InAppBrowserManagerPigeonUtils.deepHash(this.baseUrl)
    result = 31 * result + InAppBrowserManagerPigeonUtils.deepHash(this.historyUrl)
    result = 31 * result + InAppBrowserManagerPigeonUtils.deepHash(this.settings)
    result = 31 * result + InAppBrowserManagerPigeonUtils.deepHash(this.contextMenu)
    result = 31 * result + InAppBrowserManagerPigeonUtils.deepHash(this.windowId)
    result = 31 * result + InAppBrowserManagerPigeonUtils.deepHash(this.initialUserScripts)
    result = 31 * result + InAppBrowserManagerPigeonUtils.deepHash(this.pullToRefreshSettings)
    result = 31 * result + InAppBrowserManagerPigeonUtils.deepHash(this.menuItems)
    return result
  }
  override fun toString(): String {
    return "InAppBrowserOpenRequestData(id=$id, urlRequest=$urlRequest, assetFilePath=$assetFilePath, data=$data, mimeType=$mimeType, encoding=$encoding, baseUrl=$baseUrl, historyUrl=$historyUrl, settings=$settings, contextMenu=$contextMenu, windowId=$windowId, initialUserScripts=$initialUserScripts, pullToRefreshSettings=$pullToRefreshSettings, menuItems=$menuItems)"
  }
}
private open class InAppBrowserManagerPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          InAppBrowserOpenRequestData.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is InAppBrowserOpenRequestData -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}

/**
 * Implemented by `InAppBrowserManager`.
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface InAppBrowserManagerHostApi {
  /** Starts the browser Activity. `false` when there is no host Activity to start it from. */
  fun open(request: InAppBrowserOpenRequestData): Boolean
  /**
   * Opens [url] in another app. `false` when there is no host Activity; throws when no app can
   * open it.
   */
  fun openWithSystemBrowser(url: String): Boolean

  companion object {
    /** The codec used by InAppBrowserManagerHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      InAppBrowserManagerPigeonCodec()
    }
    /** Sets up an instance of `InAppBrowserManagerHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: InAppBrowserManagerHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.InAppBrowserManagerHostApi.open$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val requestArg = args[0] as InAppBrowserOpenRequestData
            val wrapped: List<Any?> = try {
              listOf(api.open(requestArg))
            } catch (exception: Throwable) {
              InAppBrowserManagerPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.InAppBrowserManagerHostApi.openWithSystemBrowser$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val urlArg = args[0] as String
            val wrapped: List<Any?> = try {
              listOf(api.openWithSystemBrowser(urlArg))
            } catch (exception: Throwable) {
              InAppBrowserManagerPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
