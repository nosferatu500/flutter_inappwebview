// Autogenerated from Pigeon (v27.3.2), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package dev.nosferatu500.inappwebview.pigeons

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object CredentialDatabasePigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Mirrors the **Android-relevant half** of the platform interface's `URLProtectionSpace`.
 *
 * `URLProtectionSpace` declares ten fields. Five are iOS-only (`authenticationMethod`,
 * `distinguishedNames`, `receivesCredentialSecurely`, `isProxy`, `proxyType`) and the Kotlin
 * `toMap()` sent every one of them as a **literal null** on the way back:
 *
 * ```kotlin
 * "authenticationMethod" to null, "distinguishedNames" to null, …
 * ```
 *
 * Two more — `sslCertificate` and `sslError` — are real on Android but **can never be non-null on
 * this channel**: the credential database's DAO builds every row through the five-argument
 * `URLProtectionSpace(id, host, protocol, realm, port)` constructor, which sets both to null, and
 * the insert path does the same. They are populated only for the SSL/auth-challenge callbacks on
 * the WebView channel, which is a different channel. So this type names the four fields the
 * database actually stores; the Dart side rebuilds the public object with the rest left at their
 * defaults, which is exactly what arrived before.
 *
 * That is seven always-null keys per protection space removed from the wire — the outbound
 * counterpart of §160's inbound `ProxyRule` finding.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class URLProtectionSpaceData (
  /** Non-null: the platform interface declares `String host` and requires it in the constructor. */
  val host: String,
  /**
   * 🚨 **Nullable on purpose, and it is the reason this migration is also a fix.**
   *
   * The platform interface declares `String? protocol` and `int? port`, and its constructor
   * requires only `host` — so `URLProtectionSpace(host: 'example.com')` is legal and leaves both
   * null. The Kotlin handler then did `call.argument("protocol")!!` and `call.argument("port")!!`
   * inside `setHttpAuthCredential`, because `CredentialDatabase.setHttpAuthCredential` takes them
   * non-null.
   *
   * Measured on an API 37 device against the pre-migration code, that combination produced:
   * `PlatformException(error, null, null, java.lang.NullPointerException)` — an opaque envelope
   * with a null message and null details, telling the caller nothing.
   *
   * Typing them nullable keeps the wire faithful to what the public API can express, and the
   * Kotlin side now rejects the case with a message that names the two fields. See
   * [CredentialDatabaseHostApi.setHttpAuthCredential].
   *
   * `getHttpAuthCredentials`, `removeHttpAuthCredential` and `removeHttpAuthCredentials` always
   * accepted null here — they forward to a `find(host?, protocol?, realm?, port?)` lookup — and
   * keep doing so.
   */
  val protocol: String? = null,
  val realm: String? = null,
  /**
   * See [protocol]. Crosses as a Dart `int`, which Pigeon maps to Kotlin `Long`, so the manager
   * narrows it with `.toInt()` for the `Int` the DAO expects (§162).
   */
  val port: Long? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): URLProtectionSpaceData {
      val host = pigeonVar_list[0] as String
      val protocol = pigeonVar_list[1] as String?
      val realm = pigeonVar_list[2] as String?
      val port = pigeonVar_list[3] as Long?
      return URLProtectionSpaceData(host, protocol, realm, port)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      host,
      protocol,
      realm,
      port,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as URLProtectionSpaceData
    return CredentialDatabasePigeonUtils.deepEquals(this.host, other.host) && CredentialDatabasePigeonUtils.deepEquals(this.protocol, other.protocol) && CredentialDatabasePigeonUtils.deepEquals(this.realm, other.realm) && CredentialDatabasePigeonUtils.deepEquals(this.port, other.port)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + CredentialDatabasePigeonUtils.deepHash(this.host)
    result = 31 * result + CredentialDatabasePigeonUtils.deepHash(this.protocol)
    result = 31 * result + CredentialDatabasePigeonUtils.deepHash(this.realm)
    result = 31 * result + CredentialDatabasePigeonUtils.deepHash(this.port)
    return result
  }
  override fun toString(): String {
    return "URLProtectionSpaceData(host=$host, protocol=$protocol, realm=$realm, port=$port)"
  }
}

/**
 * Mirrors the **Android-relevant half** of the platform interface's `URLCredential`.
 *
 * `certificates` and `persistence` are iOS-only and the Kotlin `toMap()` sent both as literal
 * nulls; they are omitted here for the same reason as [URLProtectionSpaceData]'s five.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class URLCredentialData (
  val username: String? = null,
  val password: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): URLCredentialData {
      val username = pigeonVar_list[0] as String?
      val password = pigeonVar_list[1] as String?
      return URLCredentialData(username, password)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      username,
      password,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as URLCredentialData
    return CredentialDatabasePigeonUtils.deepEquals(this.username, other.username) && CredentialDatabasePigeonUtils.deepEquals(this.password, other.password)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + CredentialDatabasePigeonUtils.deepHash(this.username)
    result = 31 * result + CredentialDatabasePigeonUtils.deepHash(this.password)
    return result
  }
  override fun toString(): String {
    return "URLCredentialData(username=$username, password=$password)"
  }
}

/**
 * Mirrors the platform interface's `URLProtectionSpaceHttpAuthCredentials`.
 *
 * Both fields are non-null here even though the public type declares them nullable: this is only
 * ever built by the host from a database row it has just read, so there is no way for either to be
 * absent. Making that explicit means the Dart side does no null-handling for a case the host
 * cannot produce.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class URLProtectionSpaceHttpAuthCredentialsData (
  val protectionSpace: URLProtectionSpaceData,
  val credentials: List<URLCredentialData>
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): URLProtectionSpaceHttpAuthCredentialsData {
      val protectionSpace = pigeonVar_list[0] as URLProtectionSpaceData
      val credentials = pigeonVar_list[1] as List<URLCredentialData>
      return URLProtectionSpaceHttpAuthCredentialsData(protectionSpace, credentials)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      protectionSpace,
      credentials,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as URLProtectionSpaceHttpAuthCredentialsData
    return CredentialDatabasePigeonUtils.deepEquals(this.protectionSpace, other.protectionSpace) && CredentialDatabasePigeonUtils.deepEquals(this.credentials, other.credentials)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + CredentialDatabasePigeonUtils.deepHash(this.protectionSpace)
    result = 31 * result + CredentialDatabasePigeonUtils.deepHash(this.credentials)
    return result
  }
  override fun toString(): String {
    return "URLProtectionSpaceHttpAuthCredentialsData(protectionSpace=$protectionSpace, credentials=$credentials)"
  }
}
private open class CredentialDatabasePigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          URLProtectionSpaceData.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          URLCredentialData.fromList(it)
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          URLProtectionSpaceHttpAuthCredentialsData.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is URLProtectionSpaceData -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is URLCredentialData -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      is URLProtectionSpaceHttpAuthCredentialsData -> {
        stream.write(131)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}

/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface CredentialDatabaseHostApi {
  /**
   * Every protection space in the database, each with its credentials.
   *
   * Returns an empty list when there is no database, which is what the hand-written channel did —
   * it built the list before the null check and answered with whatever it had.
   */
  fun getAllAuthCredentials(): List<URLProtectionSpaceHttpAuthCredentialsData>
  /** The credentials stored for [protectionSpace], or an empty list if it is not in the database. */
  fun getHttpAuthCredentials(protectionSpace: URLProtectionSpaceData): List<URLCredentialData>
  /**
   * Stores [credential] against [protectionSpace], returning whether it was stored.
   *
   * `false` means there was no database. **Throws** — with a message naming the fields — when
   * `protectionSpace.protocol` or `.port` is null, because the database keys rows on both and
   * cannot store a row without them. That replaces the bare `NullPointerException` the
   * force-unwrapped Kotlin produced; see [URLProtectionSpaceData.protocol] for the measurement.
   *
   * The Dart side discards the bool because the platform interface declares
   * `Future<void> setHttpAuthCredential(...)`; it stays on the wire so the no-database case
   * remains distinguishable, as in §157/§160/§162.
   */
  fun setHttpAuthCredential(protectionSpace: URLProtectionSpaceData, credential: URLCredentialData): Boolean
  /**
   * Removes one credential. Silently does nothing if the protection space or credential is not
   * found — a lookup miss is not an error.
   */
  fun removeHttpAuthCredential(protectionSpace: URLProtectionSpaceData, credential: URLCredentialData): Boolean
  /** Removes the protection space and every credential under it. */
  fun removeHttpAuthCredentials(protectionSpace: URLProtectionSpaceData): Boolean
  /**
   * Clears the plugin's credential database **and** the system
   * `WebViewDatabase.clearHttpAuthUsernamePassword()`. Both, as before — the second is what
   * WebView itself consults, so clearing only the plugin's table would leave auth working.
   */
  fun clearAllAuthCredentials(): Boolean

  companion object {
    /** The codec used by CredentialDatabaseHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      CredentialDatabasePigeonCodec()
    }
    /** Sets up an instance of `CredentialDatabaseHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: CredentialDatabaseHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.CredentialDatabaseHostApi.getAllAuthCredentials$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.getAllAuthCredentials())
            } catch (exception: Throwable) {
              CredentialDatabasePigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.CredentialDatabaseHostApi.getHttpAuthCredentials$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val protectionSpaceArg = args[0] as URLProtectionSpaceData
            val wrapped: List<Any?> = try {
              listOf(api.getHttpAuthCredentials(protectionSpaceArg))
            } catch (exception: Throwable) {
              CredentialDatabasePigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.CredentialDatabaseHostApi.setHttpAuthCredential$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val protectionSpaceArg = args[0] as URLProtectionSpaceData
            val credentialArg = args[1] as URLCredentialData
            val wrapped: List<Any?> = try {
              listOf(api.setHttpAuthCredential(protectionSpaceArg, credentialArg))
            } catch (exception: Throwable) {
              CredentialDatabasePigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.CredentialDatabaseHostApi.removeHttpAuthCredential$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val protectionSpaceArg = args[0] as URLProtectionSpaceData
            val credentialArg = args[1] as URLCredentialData
            val wrapped: List<Any?> = try {
              listOf(api.removeHttpAuthCredential(protectionSpaceArg, credentialArg))
            } catch (exception: Throwable) {
              CredentialDatabasePigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.CredentialDatabaseHostApi.removeHttpAuthCredentials$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val protectionSpaceArg = args[0] as URLProtectionSpaceData
            val wrapped: List<Any?> = try {
              listOf(api.removeHttpAuthCredentials(protectionSpaceArg))
            } catch (exception: Throwable) {
              CredentialDatabasePigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.CredentialDatabaseHostApi.clearAllAuthCredentials$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.clearAllAuthCredentials())
            } catch (exception: Throwable) {
              CredentialDatabasePigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
