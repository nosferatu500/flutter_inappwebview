// Autogenerated from Pigeon (v27.3.2), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package dev.nosferatu500.inappwebview.pigeons

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object ChromeCustomTabsPigeonUtils {

  fun createConnectionError(channelName: String): FlutterError {
    return FlutterError("channel-error",  "Unable to establish connection on channel: '$channelName'.", "")  }

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * An Android resource reference, mirroring the public `AndroidResource`.
 *
 * See checklist item 5: this type is shared with `InAppBrowserMenuItem` and
 * `ChromeCustomTabsSettings`, so the name is expected to collide when `InAppBrowserManager`
 * migrates. That is recorded rather than worked around.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class AndroidResourceData (
  val name: String,
  val defType: String? = null,
  val defPackage: String? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): AndroidResourceData {
      val name = pigeonVar_list[0] as String
      val defType = pigeonVar_list[1] as String?
      val defPackage = pigeonVar_list[2] as String?
      return AndroidResourceData(name, defType, defPackage)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      name,
      defType,
      defPackage,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as AndroidResourceData
    return ChromeCustomTabsPigeonUtils.deepEquals(this.name, other.name) && ChromeCustomTabsPigeonUtils.deepEquals(this.defType, other.defType) && ChromeCustomTabsPigeonUtils.deepEquals(this.defPackage, other.defPackage)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + ChromeCustomTabsPigeonUtils.deepHash(this.name)
    result = 31 * result + ChromeCustomTabsPigeonUtils.deepHash(this.defType)
    result = 31 * result + ChromeCustomTabsPigeonUtils.deepHash(this.defPackage)
    return result
  }
  override fun toString(): String {
    return "AndroidResourceData(name=$name, defType=$defType, defPackage=$defPackage)"
  }
}

/**
 * The remote-views toolbar shown below a Custom Tab.
 *
 * 🚨 **`clickableIDs` is flattened to a plain list of resources, and that is not a shortcut.** The
 * public `ChromeSafariBrowserSecondaryToolbarClickableID` has two fields -- an `id` and an
 * `onClick` handler -- but `onClick` is a Dart closure and has never crossed the wire: its
 * `toMap()` emits `{"id": …}` and nothing else, and the Kotlin reads exactly
 * `clickableIDMap["id"]`. So the wrapper object was a one-key envelope on both sides. Flattening
 * removes it; the Dart side keeps the handlers, which is where they were always kept and where the
 * `onSecondaryItemActionPerform` event dispatches to them by name.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class CustomTabsSecondaryToolbarData (
  val layout: AndroidResourceData,
  val clickableIDs: List<AndroidResourceData>
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): CustomTabsSecondaryToolbarData {
      val layout = pigeonVar_list[0] as AndroidResourceData
      val clickableIDs = pigeonVar_list[1] as List<AndroidResourceData>
      return CustomTabsSecondaryToolbarData(layout, clickableIDs)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      layout,
      clickableIDs,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as CustomTabsSecondaryToolbarData
    return ChromeCustomTabsPigeonUtils.deepEquals(this.layout, other.layout) && ChromeCustomTabsPigeonUtils.deepEquals(this.clickableIDs, other.clickableIDs)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + ChromeCustomTabsPigeonUtils.deepHash(this.layout)
    result = 31 * result + ChromeCustomTabsPigeonUtils.deepHash(this.clickableIDs)
    return result
  }
  override fun toString(): String {
    return "CustomTabsSecondaryToolbarData(layout=$layout, clickableIDs=$clickableIDs)"
  }
}
private open class ChromeCustomTabsPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          AndroidResourceData.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          CustomTabsSecondaryToolbarData.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is AndroidResourceData -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is CustomTabsSecondaryToolbarData -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}

/**
 * Dart -> platform, one channel per Custom Tabs activity instance.
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface ChromeCustomTabsHostApi {
  /**
   * Loads [url] in the already-open Custom Tab.
   *
   * Answers `false` when the activity is gone or [url] is null — the hand-written handler required
   * both and answered `false` otherwise. Preserved.
   */
  fun launchUrl(url: String, headers: Map<String, String>?, referrer: String?, otherLikelyURLs: List<String>?): Boolean
  /** Asks Chrome to pre-warm [url]. Answers Chrome's own answer, or `false` with no activity. */
  fun mayLaunchUrl(url: String?, otherLikelyURLs: List<String>?): Boolean
  /**
   * Replaces the toolbar action button's icon and description.
   *
   * [icon] is a `Uint8List` and crosses as raw bytes; the hand-written channel sent the same
   * `ByteArray` through the standard codec.
   */
  fun updateActionButton(icon: ByteArray, description: String): Boolean
  /**
   * Asks Chrome to verify a digital-asset-link relation between this app and [origin].
   *
   * 🚨 **The one inbound `int` on this channel, and §162's lint trap is aimed straight at it.**
   * Pigeon maps Dart `int` to Kotlin `Long`; androidx's
   * `CustomTabsSession.validateRelationship(@Relation int, Uri, Bundle)` wants an `Int`, and
   * `@Relation` is an `@IntDef`. §162 found that the obvious `.toInt()` narrowing **compiles
   * cleanly and fails `lintDebug` with `[WrongConstant]`**, because lint cannot see that a runtime
   * value is one of the permitted constants. Whether it fires here is measured at the lint gate,
   * not predicted — §162's own fix (a per-element loop) was specific to a *flag* IntDef and has no
   * analogue for a single value.
   *
   * Answers `false` when there is no session. Note that `true` means only that the request was
   * *accepted*; the verdict arrives on [ChromeCustomTabsFlutterApi.onRelationshipValidationResult],
   * and §178 measured that it never arrives for this fork at all — the asset-links file for
   * `inappwebview.dev` delegates to the upstream package, so the test covering this is skipped.
   */
  fun validateRelationship(relation: Long, origin: String): Boolean
  /** Replaces the secondary toolbar. Answers `false` when the activity is gone. */
  fun updateSecondaryToolbar(secondaryToolbar: CustomTabsSecondaryToolbarData): Boolean
  /**
   * Asks Chrome to open a post-message channel from [sourceOrigin].
   *
   * 🚨 **`true` here does not mean a channel exists** — it means the request was accepted.
   * [ChromeCustomTabsFlutterApi.onMessageChannelReady] is what says a channel is open, and Chrome
   * only fires it after verifying digital asset links. §178 measured the gap: this method answered
   * `true` and `onMessageChannelReady` never fired, hanging its test for a full 60 seconds.
   *
   * **That test is now skipped**, so this method and [postMessage] have *no* device coverage — see
   * the Residue note in §178. Do not read a green `chrome_safari_browser` run as covering them.
   */
  fun requestPostMessageChannel(sourceOrigin: String, targetOrigin: String?): Boolean
  /**
   * Posts a message through an open channel.
   *
   * Answers `CustomTabsService`'s own result code, and **`RESULT_FAILURE_MESSAGING_ERROR` when
   * there is no session** — a value, not a `false`, which is why this returns `int` rather than
   * `bool`. Preserved exactly; the Dart side maps it to `CustomTabsPostMessageResultType`.
   */
  fun postMessage(message: String): Long
  /**
   * Whether Chrome will deliver engagement signals for this session.
   *
   * The hand-written handler wrapped this in `try`/`catch (Throwable)` answering `false`, because
   * the androidx call throws on builds that do not implement it. Preserved rather than allowed to
   * propagate: turning "unsupported" into a `PlatformException` would be a behaviour change.
   */
  fun isEngagementSignalsApiAvailable(): Boolean
  /** Closes the Custom Tab and brings the host activity back. Answers `false` when already gone. */
  fun close(): Boolean

  companion object {
    /** The codec used by ChromeCustomTabsHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      ChromeCustomTabsPigeonCodec()
    }
    /** Sets up an instance of `ChromeCustomTabsHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: ChromeCustomTabsHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsHostApi.launchUrl$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val urlArg = args[0] as String
            val headersArg = args[1] as Map<String, String>?
            val referrerArg = args[2] as String?
            val otherLikelyURLsArg = args[3] as List<String>?
            val wrapped: List<Any?> = try {
              listOf(api.launchUrl(urlArg, headersArg, referrerArg, otherLikelyURLsArg))
            } catch (exception: Throwable) {
              ChromeCustomTabsPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsHostApi.mayLaunchUrl$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val urlArg = args[0] as String?
            val otherLikelyURLsArg = args[1] as List<String>?
            val wrapped: List<Any?> = try {
              listOf(api.mayLaunchUrl(urlArg, otherLikelyURLsArg))
            } catch (exception: Throwable) {
              ChromeCustomTabsPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsHostApi.updateActionButton$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val iconArg = args[0] as ByteArray
            val descriptionArg = args[1] as String
            val wrapped: List<Any?> = try {
              listOf(api.updateActionButton(iconArg, descriptionArg))
            } catch (exception: Throwable) {
              ChromeCustomTabsPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsHostApi.validateRelationship$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val relationArg = args[0] as Long
            val originArg = args[1] as String
            val wrapped: List<Any?> = try {
              listOf(api.validateRelationship(relationArg, originArg))
            } catch (exception: Throwable) {
              ChromeCustomTabsPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsHostApi.updateSecondaryToolbar$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val secondaryToolbarArg = args[0] as CustomTabsSecondaryToolbarData
            val wrapped: List<Any?> = try {
              listOf(api.updateSecondaryToolbar(secondaryToolbarArg))
            } catch (exception: Throwable) {
              ChromeCustomTabsPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsHostApi.requestPostMessageChannel$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val sourceOriginArg = args[0] as String
            val targetOriginArg = args[1] as String?
            val wrapped: List<Any?> = try {
              listOf(api.requestPostMessageChannel(sourceOriginArg, targetOriginArg))
            } catch (exception: Throwable) {
              ChromeCustomTabsPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsHostApi.postMessage$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val messageArg = args[0] as String
            val wrapped: List<Any?> = try {
              listOf(api.postMessage(messageArg))
            } catch (exception: Throwable) {
              ChromeCustomTabsPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsHostApi.isEngagementSignalsApiAvailable$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.isEngagementSignalsApiAvailable())
            } catch (exception: Throwable) {
              ChromeCustomTabsPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsHostApi.close$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.close())
            } catch (exception: Throwable) {
              ChromeCustomTabsPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
/**
 * Platform -> Dart, on the same per-instance channel.
 *
 * 🚨 **Eleven of these thirteen names collide with the public API, so none of them can be
 * implemented directly on the controller.** `PlatformChromeSafariBrowserEvents` declares
 * `onServiceConnected`, `onOpened`, `onNavigationEvent` and the rest as **methods with domain
 * types** — `onNavigationEvent(CustomTabsNavigationEventType?)`, not `onNavigationEvent(int)` — and
 * users override them. A class cannot both inherit those and implement the generated ones.
 *
 * So the events go through a private forwarder that converts each wire value into its domain type
 * before calling the public method, exactly as §14 did for `onFindResultReceived` and §177 for
 * `onComplete`. This is the third time P0a item 4's prediction has landed, and the largest: it is
 * the difference between a schema that compiles and one that does not.
 *
 * Note also checklist item 7: a `messageChannelSuffix` mismatch on *this* direction fails
 * **silently** (§177 measured the asymmetry), and thirteen events is a lot of silence.
 *
 * Generated class from Pigeon that represents Flutter messages that can be called from Kotlin.
 */
class ChromeCustomTabsFlutterApi(private val binaryMessenger: BinaryMessenger, private val messageChannelSuffix: String = "") {
  companion object {
    /** The codec used by ChromeCustomTabsFlutterApi. */
    val codec: MessageCodec<Any?> by lazy {
      ChromeCustomTabsPigeonCodec()
    }
  }
  fun onServiceConnected(callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsFlutterApi.onServiceConnected$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(null) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(ChromeCustomTabsPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
  fun onOpened(callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsFlutterApi.onOpened$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(null) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(ChromeCustomTabsPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
  /**
   * 🚨 **Takes no argument, although the public callback does.**
   * `PlatformChromeSafariBrowserEvents.onCompletedInitialLoad(bool? didLoadSuccessfully)` is read
   * from `call.arguments["didLoadSuccessfully"]` today — and the Kotlin sends
   * `hashMapOf<String, Any?>()`, an **empty map**. So the value has always been null on Android; it
   * is an iOS-supplied field on a shared callback.
   *
   * Modelling it would put a permanently-null argument on the wire, which is §160's finding
   * recreated. The forwarder passes `null` explicitly instead, which is exactly what an absent key
   * already produced — the same call §177 made for `PrintJobInfo`'s fourteen.
   */
  fun onCompletedInitialLoad(callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsFlutterApi.onCompletedInitialLoad$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(null) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(ChromeCustomTabsPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
  /**
   * `int` on the wire, `CustomTabsNavigationEventType?` in the public callback — the conversion is
   * the forwarder's job and is the reason the forwarder exists.
   */
  fun onNavigationEvent(navigationEventArg: Long, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsFlutterApi.onNavigationEvent$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(navigationEventArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(ChromeCustomTabsPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
  fun onClosed(callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsFlutterApi.onClosed$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(null) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(ChromeCustomTabsPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
  /**
   * The action button's id, plus the page it was pressed on.
   *
   * [url] and [title] are nullable because the Kotlin signature is
   * `onItemActionPerform(id: Int, url: String?, title: String?)`. Note the Dart side reads them as
   * **non-null** today (`String url = call.arguments["url"]`), so a null has always been an
   * implicit-cast crash rather than a handled case. The forwarder preserves that rather than
   * quietly fixing it — a null here is a platform contract question, not a transport one.
   */
  fun onItemActionPerform(idArg: Long, urlArg: String?, titleArg: String?, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsFlutterApi.onItemActionPerform$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(idArg, urlArg, titleArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(ChromeCustomTabsPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
  /**
   * [name] is the resource *name* of the secondary-toolbar view that was clicked; the Dart side
   * matches it against the `clickableIDs` it registered and calls that entry's `onClick`.
   */
  fun onSecondaryItemActionPerform(nameArg: String?, urlArg: String?, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsFlutterApi.onSecondaryItemActionPerform$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(nameArg, urlArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(ChromeCustomTabsPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
  /**
   * The verdict [ChromeCustomTabsHostApi.validateRelationship] asked for. Never fires in this fork
   * — see that method.
   */
  fun onRelationshipValidationResult(relationArg: Long, requestedOriginArg: String, resultArg: Boolean, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsFlutterApi.onRelationshipValidationResult$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(relationArg, requestedOriginArg, resultArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(ChromeCustomTabsPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
  /**
   * Fires only after Chrome has verified asset links; see
   * [ChromeCustomTabsHostApi.requestPostMessageChannel].
   */
  fun onMessageChannelReady(callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsFlutterApi.onMessageChannelReady$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(null) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(ChromeCustomTabsPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
  fun onPostMessage(messageArg: String, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsFlutterApi.onPostMessage$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(messageArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(ChromeCustomTabsPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
  fun onVerticalScrollEvent(isDirectionUpArg: Boolean, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsFlutterApi.onVerticalScrollEvent$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(isDirectionUpArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(ChromeCustomTabsPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
  fun onGreatestScrollPercentageIncreased(scrollPercentageArg: Long, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsFlutterApi.onGreatestScrollPercentageIncreased$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(scrollPercentageArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(ChromeCustomTabsPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
  fun onSessionEnded(didUserInteractArg: Boolean, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_inappwebview_android.ChromeCustomTabsFlutterApi.onSessionEnded$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(didUserInteractArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(ChromeCustomTabsPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
}
