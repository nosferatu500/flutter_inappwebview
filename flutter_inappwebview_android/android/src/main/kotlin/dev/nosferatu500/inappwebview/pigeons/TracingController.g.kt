// Autogenerated from Pigeon (v27.3.2), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package dev.nosferatu500.inappwebview.pigeons

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object TracingControllerPigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Mirrors the platform interface's `TracingSettings`.
 *
 * **`categories` is deliberately split into two typed lists.** The platform-interface type
 * declares `List<dynamic> categories` holding `String`s and `TracingCategory`s, and serialised it
 * to a heterogeneous `List` of `String`s and `int`s. The Kotlin side then took `List<Any?>` and
 * re-discovered each element's type at runtime:
 *
 * ```kotlin
 * if (category is String) builder.addCategories(category)
 * if (category is Int) builder.addCategories(category)
 * ```
 *
 * Those are **two different androidx overloads** — `addCategories(vararg String)` for category
 * name patterns and `addCategories(vararg Int)` for the predefined `TracingConfig.CATEGORIES_*`
 * constants — so the heterogeneous list was only ever a way of posting both through one door and
 * sorting them on arrival. Naming them separately makes the wire typed, deletes the runtime type
 * tests, and removes a **silent drop**: an element that was neither a `String` nor an `Int` matched
 * neither `if` and vanished with no error. `null` was such an element, and the platform interface's
 * own serialiser can produce one (its `EnumMethod.name` branch yields `null` for a
 * `TracingCategory`).
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class TracingSettingsData (
  /**
   * Category name patterns, e.g. `"blink*"` or `"renderer.scheduler"`. Passed to androidx's
   * `addCategories(String...)`.
   */
  val categoryNames: List<String>,
  /**
   * `TracingCategory` native values — the `TracingConfig.CATEGORIES_*` constants. Passed to
   * androidx's `addCategories(int...)`.
   */
  val predefinedCategories: List<Long>,
  /**
   * `TracingMode`'s native value. Null means "not set explicitly", which androidx treats as
   * `RECORD_CONTINUOUSLY`; the builder's `setTracingMode` is only called when this is non-null,
   * matching the hand-written channel's `?.let`.
   */
  val tracingMode: Long? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): TracingSettingsData {
      val categoryNames = pigeonVar_list[0] as List<String>
      val predefinedCategories = pigeonVar_list[1] as List<Long>
      val tracingMode = pigeonVar_list[2] as Long?
      return TracingSettingsData(categoryNames, predefinedCategories, tracingMode)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      categoryNames,
      predefinedCategories,
      tracingMode,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as TracingSettingsData
    return TracingControllerPigeonUtils.deepEquals(this.categoryNames, other.categoryNames) && TracingControllerPigeonUtils.deepEquals(this.predefinedCategories, other.predefinedCategories) && TracingControllerPigeonUtils.deepEquals(this.tracingMode, other.tracingMode)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + TracingControllerPigeonUtils.deepHash(this.categoryNames)
    result = 31 * result + TracingControllerPigeonUtils.deepHash(this.predefinedCategories)
    result = 31 * result + TracingControllerPigeonUtils.deepHash(this.tracingMode)
    return result
  }
  override fun toString(): String {
    return "TracingSettingsData(categoryNames=$categoryNames, predefinedCategories=$predefinedCategories, tracingMode=$tracingMode)"
  }
}
private open class TracingControllerPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          TracingSettingsData.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is TracingSettingsData -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}

/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface TracingControllerHostApi {
  /**
   * Whether tracing is currently active.
   *
   * `false` also covers "androidx does not support `TRACING_CONTROLLER_BASIC_USAGE`", which is
   * indistinguishable from "not tracing" and was already conflated by the channel this replaces.
   */
  fun isTracing(): Boolean
  /**
   * Starts tracing, returning whether it actually started.
   *
   * `false` means the feature is unsupported. The Dart side **discards this value** because the
   * platform interface declares `Future<void> start(...)`; it stays on the wire so the unsupported
   * case remains distinguishable, as with `ProcessGlobalConfigHostApi.apply` (§157) and
   * `ProxyHostApi.setProxyOverride` (§160).
   */
  fun start(settings: TracingSettingsData): Boolean
  /**
   * Stops tracing and begins writing the trace to [filePath], returning androidx's own answer:
   * **false if the framework was not tracing when the call was made**, true otherwise.
   *
   * ⚠️ **True does not mean the trace has been written.** The write happens afterwards on an
   * executor, and for a large trace it takes seconds (§158 measured ~6s for 8.5 MB). A caller that
   * needs the file on disk must poll [isTracing] until it reports false, which is what the
   * integration test does. This is androidx's contract, not a limitation of the migration — see
   * the header for why the method is therefore *not* `@async`.
   *
   * A null [filePath] discards the trace data, per androidx.
   */
  fun stop(filePath: String?): Boolean

  companion object {
    /** The codec used by TracingControllerHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      TracingControllerPigeonCodec()
    }
    /** Sets up an instance of `TracingControllerHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: TracingControllerHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.TracingControllerHostApi.isTracing$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.isTracing())
            } catch (exception: Throwable) {
              TracingControllerPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.TracingControllerHostApi.start$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val settingsArg = args[0] as TracingSettingsData
            val wrapped: List<Any?> = try {
              listOf(api.start(settingsArg))
            } catch (exception: Throwable) {
              TracingControllerPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.TracingControllerHostApi.stop$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val filePathArg = args[0] as String?
            val wrapped: List<Any?> = try {
              listOf(api.stop(filePathArg))
            } catch (exception: Throwable) {
              TracingControllerPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
