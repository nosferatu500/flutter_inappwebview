// Autogenerated from Pigeon (v27.3.2), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package dev.nosferatu500.inappwebview.pigeons

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object WebMessagePigeonUtils {

  fun createConnectionError(channelName: String): FlutterError {
    return FlutterError("channel-error",  "Unable to establish connection on channel: '$channelName'.", "")  }

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * Mirrors the platform interface's `IWebMessagePort` as it crosses this boundary.
 *
 * The Dart `toMap()` sends exactly these two fields, and the Kotlin `WebMessagePortCompatExt`
 * reads exactly these two, so nothing is lost by naming them.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class WebMessagePortData (
  /**
   * 0 or 1 — which end of the channel. Crosses as a Dart `int`, i.e. Kotlin `Long`, so the
   * manager narrows it with `.toInt()` (§162).
   */
  val index: Long,
  val webMessageChannelId: String
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): WebMessagePortData {
      val index = pigeonVar_list[0] as Long
      val webMessageChannelId = pigeonVar_list[1] as String
      return WebMessagePortData(index, webMessageChannelId)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      index,
      webMessageChannelId,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as WebMessagePortData
    return WebMessagePigeonUtils.deepEquals(this.index, other.index) && WebMessagePigeonUtils.deepEquals(this.webMessageChannelId, other.webMessageChannelId)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + WebMessagePigeonUtils.deepHash(this.index)
    result = 31 * result + WebMessagePigeonUtils.deepHash(this.webMessageChannelId)
    return result
  }
  override fun toString(): String {
    return "WebMessagePortData(index=$index, webMessageChannelId=$webMessageChannelId)"
  }
}

/**
 * Mirrors the platform interface's `WebMessage`.
 *
 * **The payload is split into two typed fields rather than one `Object?`.** `WebMessage.data` is
 * declared `dynamic` and holds either a `String` or a `Uint8List`, keyed by [type]; the old wire
 * carried it untyped and the Kotlin side recovered it with an unchecked cast:
 *
 * ```kotlin
 * port.postMessage(WebMessageCompat(data as ByteArray, ports))   // or data?.toString()
 * ```
 *
 * Naming the two shapes separately deletes that cast and lets Pigeon carry the bytes as a real
 * `ByteArray` instead of an `Any?` that happens to be one. Same treatment as §162's category
 * split, and for the same reason: a heterogeneous field is a runtime type test waiting to happen.
 *
 * The pairing is **already an invariant of the public type** — `WebMessage`'s constructor asserts
 * that `data` is null-or-`String` exactly when `type` is `STRING`, and a `Uint8List` exactly when
 * it is `ARRAY_BUFFER` — so splitting the field records a rule that already held rather than
 * inventing one.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class WebMessageData (
  /**
   * `WebMessageType`'s native value: `0` = `STRING`, `1` = `ARRAY_BUFFER`.
   *
   * Kept even though [stringData] and [arrayBufferData] are separately typed, because it is the
   * field the public API carries and it is the only thing that distinguishes a `STRING` message
   * whose data is **null** from an absent payload. Inferring the type from which field is
   * non-null would silently change that case.
   *
   * Stays an `int` rather than a Pigeon enum, as in §160/§161: the platform interface's
   * `WebMessageType` is an open `_internal(int)` class, not a closed enum, so a generated enum
   * would reject values the public API accepts.
   */
  val type: Long,
  /**
   * Set when [type] is `STRING`. Null is legal here and means a `STRING` message with no data,
   * which androidx forwards as a null `String`.
   */
  val stringData: String? = null,
  /**
   * Set when [type] is `ARRAY_BUFFER`. Arrives on the Kotlin side as a real `ByteArray`, so
   * `WebMessageCompat(ByteArray, …)` needs no cast.
   */
  val arrayBufferData: ByteArray? = null,
  /**
   * **Inbound only.** The Kotlin `WebMessageCompatExt.toMap()` omits `ports` entirely, so an
   * event coming *from* the platform never carries them; only messages Dart posts do. Nullable
   * rather than a required empty list so that asymmetry stays visible on the type.
   */
  val ports: List<WebMessagePortData>? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): WebMessageData {
      val type = pigeonVar_list[0] as Long
      val stringData = pigeonVar_list[1] as String?
      val arrayBufferData = pigeonVar_list[2] as ByteArray?
      val ports = pigeonVar_list[3] as List<WebMessagePortData>?
      return WebMessageData(type, stringData, arrayBufferData, ports)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      type,
      stringData,
      arrayBufferData,
      ports,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as WebMessageData
    return WebMessagePigeonUtils.deepEquals(this.type, other.type) && WebMessagePigeonUtils.deepEquals(this.stringData, other.stringData) && WebMessagePigeonUtils.deepEquals(this.arrayBufferData, other.arrayBufferData) && WebMessagePigeonUtils.deepEquals(this.ports, other.ports)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + WebMessagePigeonUtils.deepHash(this.type)
    result = 31 * result + WebMessagePigeonUtils.deepHash(this.stringData)
    result = 31 * result + WebMessagePigeonUtils.deepHash(this.arrayBufferData)
    result = 31 * result + WebMessagePigeonUtils.deepHash(this.ports)
    return result
  }
  override fun toString(): String {
    return "WebMessageData(type=$type, stringData=$stringData, arrayBufferData=${arrayBufferData?.contentToString()}, ports=$ports)"
  }
}
private open class WebMessagePigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          WebMessagePortData.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          WebMessageData.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is WebMessagePortData -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is WebMessageData -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}

/**
 * Dart -> platform, one channel per `WebMessageChannel` instance via `messageChannelSuffix`.
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface WebMessageChannelHostApi {
  /**
   * Registers the port's message callback, which is what makes
   * [WebMessageChannelFlutterApi.onMessage] start firing.
   *
   * Returns false only when the underlying view is not an `InAppWebView`. Note it answers
   * **true** when `WEB_MESSAGE_PORT_SET_MESSAGE_CALLBACK` is unsupported and nothing was
   * registered — "true" here means "no error", not "the callback is live". That is the
   * hand-written channel's behaviour, preserved deliberately; changing it is a separate decision.
   */
  fun setWebMessageCallback(index: Long): Boolean
  /**
   * Posts [message] through the port at [index].
   *
   * Same `true`-when-unsupported caveat as [setWebMessageCallback].
   */
  fun postMessage(index: Long, message: WebMessageData): Boolean
  /** Closes the port at [index]. Same caveat again. */
  fun close(index: Long): Boolean

  companion object {
    /** The codec used by WebMessageChannelHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      WebMessagePigeonCodec()
    }
    /** Sets up an instance of `WebMessageChannelHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: WebMessageChannelHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.WebMessageChannelHostApi.setWebMessageCallback$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val indexArg = args[0] as Long
            val wrapped: List<Any?> = try {
              listOf(api.setWebMessageCallback(indexArg))
            } catch (exception: Throwable) {
              WebMessagePigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.WebMessageChannelHostApi.postMessage$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val indexArg = args[0] as Long
            val messageArg = args[1] as WebMessageData
            val wrapped: List<Any?> = try {
              listOf(api.postMessage(indexArg, messageArg))
            } catch (exception: Throwable) {
              WebMessagePigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.WebMessageChannelHostApi.close$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val indexArg = args[0] as Long
            val wrapped: List<Any?> = try {
              listOf(api.close(indexArg))
            } catch (exception: Throwable) {
              WebMessagePigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
/**
 * Platform -> Dart, on the same per-instance channel.
 *
 * Generated class from Pigeon that represents Flutter messages that can be called from Kotlin.
 */
class WebMessageChannelFlutterApi(private val binaryMessenger: BinaryMessenger, private val messageChannelSuffix: String = "") {
  companion object {
    /** The codec used by WebMessageChannelFlutterApi. */
    val codec: MessageCodec<Any?> by lazy {
      WebMessagePigeonCodec()
    }
  }
  /**
   * [message] is nullable because androidx's `WebMessageCallbackCompat.onMessage` may deliver a
   * null `WebMessageCompat`; the Dart side forwards null to the port's `onMessage` callback.
   */
  fun onMessage(indexArg: Long, messageArg: WebMessageData?, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_inappwebview_android.WebMessageChannelFlutterApi.onMessage$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(indexArg, messageArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(WebMessagePigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
}
/**
 * Dart -> platform, one channel per `WebMessageListener` instance.
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface WebMessageListenerHostApi {
  /**
   * Posts [message] back through the listener's `JavaScriptReplyProxy`.
   *
   * Always answers true when the view is an `InAppWebView`: the hand-written channel called
   * `result.success(true)` unconditionally at the end, including when there was no reply proxy
   * or `WEB_MESSAGE_LISTENER` was unsupported. Preserved.
   */
  fun postMessage(message: WebMessageData): Boolean

  companion object {
    /** The codec used by WebMessageListenerHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      WebMessagePigeonCodec()
    }
    /** Sets up an instance of `WebMessageListenerHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: WebMessageListenerHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.WebMessageListenerHostApi.postMessage$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val messageArg = args[0] as WebMessageData
            val wrapped: List<Any?> = try {
              listOf(api.postMessage(messageArg))
            } catch (exception: Throwable) {
              WebMessagePigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
/**
 * Platform -> Dart, on the same per-instance channel.
 *
 * Generated class from Pigeon that represents Flutter messages that can be called from Kotlin.
 */
class WebMessageListenerFlutterApi(private val binaryMessenger: BinaryMessenger, private val messageChannelSuffix: String = "") {
  companion object {
    /** The codec used by WebMessageListenerFlutterApi. */
    val codec: MessageCodec<Any?> by lazy {
      WebMessagePigeonCodec()
    }
  }
  fun onPostMessage(messageArg: WebMessageData?, sourceOriginArg: String?, isMainFrameArg: Boolean, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_inappwebview_android.WebMessageListenerFlutterApi.onPostMessage$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(messageArg, sourceOriginArg, isMainFrameArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(WebMessagePigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
}
