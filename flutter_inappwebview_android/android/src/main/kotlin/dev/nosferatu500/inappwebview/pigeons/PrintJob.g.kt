// Autogenerated from Pigeon (v27.3.2), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package dev.nosferatu500.inappwebview.pigeons

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object PrintJobPigeonUtils {

  fun createConnectionError(channelName: String): FlutterError {
    return FlutterError("channel-error",  "Unable to establish connection on channel: '$channelName'.", "")  }

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
  fun doubleEquals(a: Double, b: Double): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0) 0.0 else a) == (if (b == 0.0) 0.0 else b) || (a.isNaN() && b.isNaN())
  }

  fun floatEquals(a: Float, b: Float): Boolean {
    // Normalize -0.0 to 0.0 and handle NaN equality.
    return (if (a == 0.0f) 0.0f else a) == (if (b == 0.0f) 0.0f else b) || (a.isNaN() && b.isNaN())
  }

  fun doubleHash(d: Double): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (d == 0.0) 0.0 else d
    val bits = java.lang.Double.doubleToLongBits(normalized)
    return (bits xor (bits ushr 32)).toInt()
  }

  fun floatHash(f: Float): Int {
    // Normalize -0.0 to 0.0 and handle NaN to ensure consistent hash codes.
    val normalized = if (f == 0.0f) 0.0f else f
    return java.lang.Float.floatToIntBits(normalized)
  }

  fun deepEquals(a: Any?, b: Any?): Boolean {
    if (a === b) {
      return true
    }
    if (a == null || b == null) {
      return false
    }
    if (a is ByteArray && b is ByteArray) {
      return a.contentEquals(b)
    }
    if (a is IntArray && b is IntArray) {
      return a.contentEquals(b)
    }
    if (a is LongArray && b is LongArray) {
      return a.contentEquals(b)
    }
    if (a is DoubleArray && b is DoubleArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!doubleEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is FloatArray && b is FloatArray) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!floatEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is Array<*> && b is Array<*>) {
      if (a.size != b.size) return false
      for (i in a.indices) {
        if (!deepEquals(a[i], b[i])) return false
      }
      return true
    }
    if (a is List<*> && b is List<*>) {
      if (a.size != b.size) return false
      val iterA = a.iterator()
      val iterB = b.iterator()
      while (iterA.hasNext() && iterB.hasNext()) {
        if (!deepEquals(iterA.next(), iterB.next())) return false
      }
      return true
    }
    if (a is Map<*, *> && b is Map<*, *>) {
      if (a.size != b.size) return false
      for (entry in a) {
        val key = entry.key
        var found = false
        for (bEntry in b) {
          if (deepEquals(key, bEntry.key)) {
            if (deepEquals(entry.value, bEntry.value)) {
              found = true
              break
            } else {
              return false
            }
          }
        }
        if (!found) return false
      }
      return true
    }
    if (a is Double && b is Double) {
      return doubleEquals(a, b)
    }
    if (a is Float && b is Float) {
      return floatEquals(a, b)
    }
    return a == b
  }

  fun deepHash(value: Any?): Int {
    return when (value) {
      null -> 0
      is ByteArray -> value.contentHashCode()
      is IntArray -> value.contentHashCode()
      is LongArray -> value.contentHashCode()
      is DoubleArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + doubleHash(item)
        }
        result
      }
      is FloatArray -> {
        var result = 1
        for (item in value) {
          result = 31 * result + floatHash(item)
        }
        result
      }
      is Array<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is List<*> -> {
        var result = 1
        for (item in value) {
          result = 31 * result + deepHash(item)
        }
        result
      }
      is Map<*, *> -> {
        var result = 0
        for (entry in value) {
          result += ((deepHash(entry.key) * 31) xor deepHash(entry.value))
        }
        result
      }
      is Double -> doubleHash(value)
      is Float -> floatHash(value)
      else -> value.hashCode()
    }
  }

}

/**
 * One print job's state, mirroring the platform interface's `PrintJobInfo` **as far as Android
 * populates it**.
 *
 * 🚨 **Checklist item 9, in the opposite direction to every previous migration.** §160 found seven
 * fields per proxy rule that the wire carried and the platform never read; §163 nine per
 * credential; §168 one always-null `isSessionOnly`. Here the wire carries nothing dead — instead
 * the **reader expects fourteen fields the wire has never carried**:
 *
 *  * `PrintJobInfo.fromMap` reads 15 keys; the Kotlin `toMap()` sends **7**. The missing eight —
 *    `canSpawnSeparateThread`, `currentPage`, `firstPage`, `isCopyingOperation`, `lastPage`,
 *    `preferredRenderingQuality`, `showsPrintPanel`, `showsProgressPanel` — have **zero
 *    occurrences in the entire Android Kotlin module**, measured by grep per key.
 *  * `PrintJobAttributes.fromMap` reads 12; the Kotlin sends **6**. The missing six are
 *    `footerHeight`, `headerHeight`, `maximumContentHeight`, `maximumContentWidth`, `paperRect`
 *    and `printableRect`, all likewise absent from the Android source.
 *
 * They are iOS-only fields on a shared cross-platform type, and today they arrive as *absent map
 * keys*, which `fromMap` reads as null. So modelling only what Android sends is **exactly
 * behaviour-preserving** — the Dart side passes null for the rest when rebuilding the public type,
 * which is what an absent key already produced. It is also the §168 precedent (drop what is always
 * null) applied to the only case where the alternative was to invent 14 permanently-null wire
 * fields. §176's device test pins all eight of the `PrintJobInfo` ones as null so that a change in
 * either direction is visible.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class PrintJobInfoData (
  /** `android.print.PrintJobInfo`'s own constant, rebuilt into `PrintJobState` on the Dart side. */
  val state: Long,
  val copies: Long,
  /**
   * `info.pages?.size` — null when the job does not describe a page range, which is the common
   * case for a job that has only just been created.
   */
  val numberOfPages: Long? = null,
  /**
   * Epoch milliseconds. Already a Kotlin `Long`, so this is the one number here that needs no
   * widening (checklist item 4).
   */
  val creationTime: Long,
  val label: String,
  /**
   * 🚨 **Flattened from a nested one-key map, deliberately.** The hand-written Kotlin sent
   * `"printer" to hashMapOf("id" to printerId)` and the public `Printer` type has exactly one
   * field, `id`. The nesting bought nothing.
   *
   * Note the Dart side must still build a **non-null** `Printer` when this is null: the old map was
   * always present even when the id inside it was null, so `Printer.fromMap` always returned an
   * object. Rebuilding it as `Printer(id: printerId)` unconditionally preserves that; making the
   * `Printer` itself null when the id is null would be a behaviour change dressed up as a tidy-up.
   */
  val printerId: String? = null,
  val attributes: PrintJobAttributesData? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PrintJobInfoData {
      val state = pigeonVar_list[0] as Long
      val copies = pigeonVar_list[1] as Long
      val numberOfPages = pigeonVar_list[2] as Long?
      val creationTime = pigeonVar_list[3] as Long
      val label = pigeonVar_list[4] as String
      val printerId = pigeonVar_list[5] as String?
      val attributes = pigeonVar_list[6] as PrintJobAttributesData?
      return PrintJobInfoData(state, copies, numberOfPages, creationTime, label, printerId, attributes)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      state,
      copies,
      numberOfPages,
      creationTime,
      label,
      printerId,
      attributes,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PrintJobInfoData
    return PrintJobPigeonUtils.deepEquals(this.state, other.state) && PrintJobPigeonUtils.deepEquals(this.copies, other.copies) && PrintJobPigeonUtils.deepEquals(this.numberOfPages, other.numberOfPages) && PrintJobPigeonUtils.deepEquals(this.creationTime, other.creationTime) && PrintJobPigeonUtils.deepEquals(this.label, other.label) && PrintJobPigeonUtils.deepEquals(this.printerId, other.printerId) && PrintJobPigeonUtils.deepEquals(this.attributes, other.attributes)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.state)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.copies)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.numberOfPages)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.creationTime)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.label)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.printerId)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.attributes)
    return result
  }
  override fun toString(): String {
    return "PrintJobInfoData(state=$state, copies=$copies, numberOfPages=$numberOfPages, creationTime=$creationTime, label=$label, printerId=$printerId, attributes=$attributes)"
  }
}

/**
 * The job's print attributes, again only as far as Android populates them; see [PrintJobInfoData].
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class PrintJobAttributesData (
  val colorMode: Long,
  val duplex: Long? = null,
  /**
   * **Derived, not read.** `PrintAttributes` has no orientation; the Kotlin computes it as
   * `if (mediaSize.isPortrait) 0 else 1` and leaves it null when there is no media size at all.
   * Preserved as-is.
   */
  val orientation: Long? = null,
  val mediaSize: PrintJobMediaSizeData? = null,
  val resolution: PrintJobResolutionData? = null,
  val margins: PrintJobMarginsData? = null
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PrintJobAttributesData {
      val colorMode = pigeonVar_list[0] as Long
      val duplex = pigeonVar_list[1] as Long?
      val orientation = pigeonVar_list[2] as Long?
      val mediaSize = pigeonVar_list[3] as PrintJobMediaSizeData?
      val resolution = pigeonVar_list[4] as PrintJobResolutionData?
      val margins = pigeonVar_list[5] as PrintJobMarginsData?
      return PrintJobAttributesData(colorMode, duplex, orientation, mediaSize, resolution, margins)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      colorMode,
      duplex,
      orientation,
      mediaSize,
      resolution,
      margins,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PrintJobAttributesData
    return PrintJobPigeonUtils.deepEquals(this.colorMode, other.colorMode) && PrintJobPigeonUtils.deepEquals(this.duplex, other.duplex) && PrintJobPigeonUtils.deepEquals(this.orientation, other.orientation) && PrintJobPigeonUtils.deepEquals(this.mediaSize, other.mediaSize) && PrintJobPigeonUtils.deepEquals(this.resolution, other.resolution) && PrintJobPigeonUtils.deepEquals(this.margins, other.margins)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.colorMode)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.duplex)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.orientation)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.mediaSize)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.resolution)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.margins)
    return result
  }
  override fun toString(): String {
    return "PrintJobAttributesData(colorMode=$colorMode, duplex=$duplex, orientation=$orientation, mediaSize=$mediaSize, resolution=$resolution, margins=$margins)"
  }
}

/**
 * Matches the public `PrintJobMediaSize` field for field, in both directions — one of the three
 * leaf types on this channel where nothing is added and nothing is dropped.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class PrintJobMediaSizeData (
  val id: String,
  val label: String? = null,
  val widthMils: Long,
  val heightMils: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PrintJobMediaSizeData {
      val id = pigeonVar_list[0] as String
      val label = pigeonVar_list[1] as String?
      val widthMils = pigeonVar_list[2] as Long
      val heightMils = pigeonVar_list[3] as Long
      return PrintJobMediaSizeData(id, label, widthMils, heightMils)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      id,
      label,
      widthMils,
      heightMils,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PrintJobMediaSizeData
    return PrintJobPigeonUtils.deepEquals(this.id, other.id) && PrintJobPigeonUtils.deepEquals(this.label, other.label) && PrintJobPigeonUtils.deepEquals(this.widthMils, other.widthMils) && PrintJobPigeonUtils.deepEquals(this.heightMils, other.heightMils)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.id)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.label)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.widthMils)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.heightMils)
    return result
  }
  override fun toString(): String {
    return "PrintJobMediaSizeData(id=$id, label=$label, widthMils=$widthMils, heightMils=$heightMils)"
  }
}

/**
 * Matches the public `PrintJobResolution` field for field; see [PrintJobMediaSizeData].
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class PrintJobResolutionData (
  val id: String,
  val label: String,
  val verticalDpi: Long,
  val horizontalDpi: Long
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PrintJobResolutionData {
      val id = pigeonVar_list[0] as String
      val label = pigeonVar_list[1] as String
      val verticalDpi = pigeonVar_list[2] as Long
      val horizontalDpi = pigeonVar_list[3] as Long
      return PrintJobResolutionData(id, label, verticalDpi, horizontalDpi)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      id,
      label,
      verticalDpi,
      horizontalDpi,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PrintJobResolutionData
    return PrintJobPigeonUtils.deepEquals(this.id, other.id) && PrintJobPigeonUtils.deepEquals(this.label, other.label) && PrintJobPigeonUtils.deepEquals(this.verticalDpi, other.verticalDpi) && PrintJobPigeonUtils.deepEquals(this.horizontalDpi, other.horizontalDpi)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.id)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.label)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.verticalDpi)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.horizontalDpi)
    return result
  }
  override fun toString(): String {
    return "PrintJobResolutionData(id=$id, label=$label, verticalDpi=$verticalDpi, horizontalDpi=$horizontalDpi)"
  }
}

/**
 * The job's minimum margins, rebuilt into Flutter's `EdgeInsets` on the Dart side.
 *
 * `double`, not `int`: these are the only non-integer numbers on the channel, and they are already
 * `Double` in the Kotlin, so no conversion happens in either direction.
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class PrintJobMarginsData (
  val top: Double,
  val right: Double,
  val bottom: Double,
  val left: Double
)
 {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): PrintJobMarginsData {
      val top = pigeonVar_list[0] as Double
      val right = pigeonVar_list[1] as Double
      val bottom = pigeonVar_list[2] as Double
      val left = pigeonVar_list[3] as Double
      return PrintJobMarginsData(top, right, bottom, left)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      top,
      right,
      bottom,
      left,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other == null || other.javaClass != javaClass) {
      return false
    }
    if (this === other) {
      return true
    }
    val other = other as PrintJobMarginsData
    return PrintJobPigeonUtils.deepEquals(this.top, other.top) && PrintJobPigeonUtils.deepEquals(this.right, other.right) && PrintJobPigeonUtils.deepEquals(this.bottom, other.bottom) && PrintJobPigeonUtils.deepEquals(this.left, other.left)
  }

  override fun hashCode(): Int {
    var result = javaClass.hashCode()
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.top)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.right)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.bottom)
    result = 31 * result + PrintJobPigeonUtils.deepHash(this.left)
    return result
  }
  override fun toString(): String {
    return "PrintJobMarginsData(top=$top, right=$right, bottom=$bottom, left=$left)"
  }
}
private open class PrintJobPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PrintJobInfoData.fromList(it)
        }
      }
      130.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PrintJobAttributesData.fromList(it)
        }
      }
      131.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PrintJobMediaSizeData.fromList(it)
        }
      }
      132.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PrintJobResolutionData.fromList(it)
        }
      }
      133.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          PrintJobMarginsData.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    when (value) {
      is PrintJobInfoData -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      is PrintJobAttributesData -> {
        stream.write(130)
        writeValue(stream, value.toList())
      }
      is PrintJobMediaSizeData -> {
        stream.write(131)
        writeValue(stream, value.toList())
      }
      is PrintJobResolutionData -> {
        stream.write(132)
        writeValue(stream, value.toList())
      }
      is PrintJobMarginsData -> {
        stream.write(133)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}

/**
 * Dart -> platform, one channel per `PrintJobController` instance.
 *
 * The `messageChannelSuffix` is the job id, so the two halves must agree on it exactly; §165 proved
 * that a mismatch here produces no error at all, just a call that never arrives and a 60-second
 * timeout.
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface PrintJobControllerHostApi {
  /**
   * Cancels the job.
   *
   * Answers `false` only when the controller has already been disposed. **It does not mean the job
   * was cancelled** — `android.print.PrintJob.cancel()` is a no-op while the job is in `CREATED`
   * state, which is the state it is in for as long as the print dialog is open. Measured in §176,
   * where `getInfo().state` stays `CREATED` across a `cancel()`; the pre-existing behaviour is
   * preserved rather than corrected, because correcting it means waiting on a job state no test can
   * reach.
   */
  fun cancel(): Boolean
  /**
   * Restarts the job. Answers `false` only when the controller has been disposed.
   *
   * Like [cancel], a no-op against the only state a test can produce: `PrintJob.restart()` applies
   * to a **failed** job. Preserved as-is.
   */
  fun restart(): Boolean
  /**
   * The job's current state, or null when the controller has been disposed or holds no job.
   *
   * Null is the only signal this channel gives that a controller is finished — §176 asserts
   * `getInfo()` answers null after [dispose], which is that group's one true state transition.
   */
  fun getInfo(): PrintJobInfoData?
  /**
   * Releases the native controller and cancels the underlying job.
   *
   * Answers `false` only when it had already been disposed. Note the **facade** drops this answer
   * twice over: `PlatformPrintJobController.dispose()` is `Future<void>`, and the app-facing
   * `PrintJobController.dispose()` is plain `void`, so a caller can neither read the result nor
   * await the teardown. Recorded, not changed — both are public signatures.
   */
  fun dispose(): Boolean

  companion object {
    /** The codec used by PrintJobControllerHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      PrintJobPigeonCodec()
    }
    /** Sets up an instance of `PrintJobControllerHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: PrintJobControllerHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.PrintJobControllerHostApi.cancel$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.cancel())
            } catch (exception: Throwable) {
              PrintJobPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.PrintJobControllerHostApi.restart$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.restart())
            } catch (exception: Throwable) {
              PrintJobPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.PrintJobControllerHostApi.getInfo$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.getInfo())
            } catch (exception: Throwable) {
              PrintJobPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.PrintJobControllerHostApi.dispose$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.dispose())
            } catch (exception: Throwable) {
              PrintJobPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
/**
 * Platform -> Dart, on the same per-instance channel.
 *
 * 🚨 **This is §14's predicted name collision, arriving for the first time.** P0a item 4 has
 * carried the line *"Expect a private forwarding class per event whose name collides with a
 * platform-interface member"* since the pilot. `PlatformPrintJobController` exposes
 * `PrintJobCompletionHandler? onComplete` as a **field**, and Pigeon generates a **method** of the
 * same name — which Dart rejects as inconsistent inheritance if one class tries to be both.
 *
 * So `AndroidPrintJobController` does **not** implement this API directly. A private
 * `_PrintJobControllerFlutterApiImpl` receives the event and forwards it to the controller's
 * callback field, exactly as §14 did for `onFindResultReceived`. §165's two channels escaped this
 * only because `onMessage` and `onPostMessage` happen not to collide with anything.
 *
 * Generated class from Pigeon that represents Flutter messages that can be called from Kotlin.
 */
class PrintJobControllerFlutterApi(private val binaryMessenger: BinaryMessenger, private val messageChannelSuffix: String = "") {
  companion object {
    /** The codec used by PrintJobControllerFlutterApi. */
    val codec: MessageCodec<Any?> by lazy {
      PrintJobPigeonCodec()
    }
  }
  /**
   * Fires when the print job finishes, from `InAppWebViewPrintDocumentAdapter.onFinish()`.
   *
   * [error] is always null on Android today — the adapter's callback has no failure path — but it
   * is kept nullable because the public `PrintJobCompletionHandler` takes it and iOS supplies it.
   */
  fun onComplete(completedArg: Boolean, errorArg: String?, callback: (Result<Unit>) -> Unit)
{
    val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
    val channelName = "dev.flutter.pigeon.flutter_inappwebview_android.PrintJobControllerFlutterApi.onComplete$separatedMessageChannelSuffix"
    val channel = BasicMessageChannel<Any?>(binaryMessenger, channelName, codec)
    channel.send(listOf(completedArg, errorArg)) {
      if (it is List<*>) {
        if (it.size > 1) {
          callback(Result.failure(FlutterError(it[0] as String, it[1] as String, it[2] as String?)))
        } else {
          callback(Result.success(Unit))
        }
      } else {
        callback(Result.failure(PrintJobPigeonUtils.createConnectionError(channelName)))
      } 
    }
  }
}
