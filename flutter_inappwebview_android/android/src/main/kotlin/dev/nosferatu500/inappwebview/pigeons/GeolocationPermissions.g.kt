// Autogenerated from Pigeon (v27.3.2), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package dev.nosferatu500.inappwebview.pigeons

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMethodCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer
private object GeolocationPermissionsPigeonUtils {

  fun wrapResult(result: Any?): List<Any?> {
    return listOf(result)
  }

  fun wrapError(exception: Throwable): List<Any?> {
    return if (exception is FlutterError) {
      listOf(
        exception.code,
        exception.message,
        exception.details
      )
    } else {
      listOf(
        exception.javaClass.simpleName,
        exception.toString(),
        "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
      )
    }
  }
}
private open class GeolocationPermissionsPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return     super.readValueOfType(type, buffer)
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?)   {
    super.writeValue(stream, value)
  }
}


/** Generated interface from Pigeon that represents a handler of messages from Flutter. */
interface GeolocationPermissionsHostApi {
  /**
   * Stores a decision allowing [origin] to use the Geolocation API.
   *
   * Answers `false` when the store could not be resolved — `MULTI_PROFILE` unsupported, or no
   * profile of that name — in which case **nothing was stored**. It never falls back to the default
   * store: granting an origin location access in the wrong profile is a privacy decision applied to
   * the wrong session.
   *
   * Synchronous: `GeolocationPermissions.allow` returns `void` and the `true` is this plugin's
   * "the store was resolved and the call was made".
   */
  fun allow(origin: String, profileName: String?): Boolean
  /**
   * Removes any stored decision for [origin], so the next request from it prompts again.
   *
   * Clearing is not denying: there is no stored "deny". Answers `false` on an unresolvable store;
   * see [allow].
   */
  fun clear(origin: String, profileName: String?): Boolean
  /** Removes every stored decision. Answers `false` on an unresolvable store; see [allow]. */
  fun clearAll(profileName: String?): Boolean
  /**
   * Whether [origin] has a stored decision allowing the Geolocation API.
   *
   * 🚨 **`bool?`, and the null is load-bearing — it is the only such null on this channel.** Null
   * means the question *could not be asked* (unresolvable store); `false` means it was asked and
   * nothing is stored. Collapsing them would report "no decision" for a profile that was never
   * consulted, and a caller deciding whether to prompt on that basis would prompt wrongly.
   *
   * This is the branch the migration most had to preserve, and the one the four other methods
   * deliberately disagree with: they answer `false` (or an empty list) for the same condition.
   * Pigeon makes an unanswered branch unrepresentable, so each is restated by hand in the Kotlin.
   * §174's `an unknown profile resolves to no store` pins all five on a device.
   *
   * `@async`: `GeolocationPermissions.getAllowed` reports through a `ValueCallback<Boolean>`.
   */
  fun getAllowed(origin: String, profileName: String?, callback: (Result<Boolean?>) -> Unit)
  /**
   * The origins that have a stored decision allowing the Geolocation API.
   *
   * 🚨 **These come back normalised, with a trailing `/`** — `allow("https://x.test")` is listed
   * here as `"https://x.test/"`. Measured in §174, where it failed four tests at once and looked
   * like four problems. The other four methods accept **either** form, so listing then acting
   * works; only comparing against the string you passed to [allow] does not. The public dartdoc on
   * `getOrigins` and `allow` now says so.
   *
   * Answers an empty list on an unresolvable store — which is a *third* spelling of the same
   * condition, alongside [allow]'s `false` and [getAllowed]'s `null`. Preserved as-is rather than
   * harmonised; harmonising is a platform-interface change.
   *
   * `@async`: `GeolocationPermissions.getOrigins` reports through a `ValueCallback<Set<String>>`.
   * The platform's `Set` has no wire representation, so the Kotlin copies it into a list — order is
   * therefore whatever the set iterates, and nothing should depend on it.
   */
  fun getOrigins(profileName: String?, callback: (Result<List<String>>) -> Unit)

  companion object {
    /** The codec used by GeolocationPermissionsHostApi. */
    val codec: MessageCodec<Any?> by lazy {
      GeolocationPermissionsPigeonCodec()
    }
    /** Sets up an instance of `GeolocationPermissionsHostApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: GeolocationPermissionsHostApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.GeolocationPermissionsHostApi.allow$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val originArg = args[0] as String
            val profileNameArg = args[1] as String?
            val wrapped: List<Any?> = try {
              listOf(api.allow(originArg, profileNameArg))
            } catch (exception: Throwable) {
              GeolocationPermissionsPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.GeolocationPermissionsHostApi.clear$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val originArg = args[0] as String
            val profileNameArg = args[1] as String?
            val wrapped: List<Any?> = try {
              listOf(api.clear(originArg, profileNameArg))
            } catch (exception: Throwable) {
              GeolocationPermissionsPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.GeolocationPermissionsHostApi.clearAll$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val profileNameArg = args[0] as String?
            val wrapped: List<Any?> = try {
              listOf(api.clearAll(profileNameArg))
            } catch (exception: Throwable) {
              GeolocationPermissionsPigeonUtils.wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.GeolocationPermissionsHostApi.getAllowed$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val originArg = args[0] as String
            val profileNameArg = args[1] as String?
            api.getAllowed(originArg, profileNameArg) { result: Result<Boolean?> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(GeolocationPermissionsPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(GeolocationPermissionsPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.flutter_inappwebview_android.GeolocationPermissionsHostApi.getOrigins$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { message, reply ->
            val args = message as List<Any?>
            val profileNameArg = args[0] as String?
            api.getOrigins(profileNameArg) { result: Result<List<String>> ->
              val error = result.exceptionOrNull()
              if (error != null) {
                reply.reply(GeolocationPermissionsPigeonUtils.wrapError(error))
              } else {
                val data = result.getOrNull()
                reply.reply(GeolocationPermissionsPigeonUtils.wrapResult(data))
              }
            }
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}
