<?xml version="1.0" encoding="utf-8"?>
<!--
  Do not "simplify" this hierarchy. InAppWebView.rebuildActionMode() navigates it by child index:

      HorizontalScrollView hsv = (HorizontalScrollView) floatingContextMenu.getChildAt(0);
      LinearLayout menuItemList = (LinearLayout) hsv.getChildAt(0);

  so the HorizontalScrollView (UselessParent) and the inner LinearLayout (UselessLeaf) are both
  load-bearing. The inner LinearLayout looks empty only because its menu items are inflated at
  runtime from floating_action_mode_item.xml; the HorizontalScrollView gives them scrolling when
  they overflow the screen width. Removing either would break the floating context menu at runtime
  with no compile-time error.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:alpha="0"
    android:background="@drawable/floating_action_mode_shape"
    android:elevation="4dp">
    <HorizontalScrollView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:ignore="UselessParent">
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            tools:ignore="UselessLeaf"></LinearLayout>
    </HorizontalScrollView>
</LinearLayout>
